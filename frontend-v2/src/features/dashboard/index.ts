/* Feature dashboard: vista de actividad de un evento, compartida por el Admin
 * (Dashboard General) y el Cliente (su evento aprobado, en solo lectura). */
export { DashboardEvento } from './DashboardEvento';
